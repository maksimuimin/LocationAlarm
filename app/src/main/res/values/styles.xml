<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="listPrimaryText">
        <item name="android:textColor">@color/colorListPrimary</item>
        <item name="android:textSize">@dimen/primaryTextSize</item>
        <item name="android:fontFamily">sans-serif-medium</item>
    </style>

    <style name="listSecondaryText">
        <item name="android:textColor">@color/colorListSecondary</item>
        <item name="android:textSize">@dimen/secondaryTextSize</item>
        <item name="android:layout_height">@dimen/secondaryTextHeight</item>
        <item name="android:fontFamily">sans-serif-thin</item>
    </style>

    <style name="NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>
